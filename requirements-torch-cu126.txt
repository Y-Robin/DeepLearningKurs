# NVIDIA GPU build for Windows/Linux (CUDA 12.6 runtime is bundled in the wheel).
# Requires a supported NVIDIA driver. Do not install this together with
# requirements-torch-cpu.txt.
--index-url https://download.pytorch.org/whl/cu126
torch==2.7.1
torchvision==0.22.1
