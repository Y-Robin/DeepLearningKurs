# Keep Conda responsible only for the interpreter. All Python packages are
# installed by pip from the pinned requirements files, avoiding mixed binary
# packages from Conda and pip in one environment.
name: deep-learning-kurs
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
